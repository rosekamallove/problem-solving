#include<iostream>
#include<climits>
using namespace std;

/*
 * Given an array of integers,
 * find the subarray with its sum being equal to k(given)
 */

int main()
{
    int n, m; cin>>n>>m;
    int arr[n];

    for(int i=0; i<n; i++)
        cin>>arr[i];
}
