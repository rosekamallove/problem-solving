#include<iostream>
using namespace std;

/* 
 * We can use the binary search STL Function
 *
 * binary_search(a, a+n, target);
 * returns true or false.
 *
 * we can use lower_bound or upper_bound to get the index if we want
 *
 * lower_bound: retur itr if found
 * upper_bound: retur next_itr if found
 *
 * lower_bound: retur next_itr if found
 * upper_bound: retur next_itr if found
 */

int main(){
}

