#include<iostream>
using namespace std;

/* Primality Check
 *
 * Brute Force:
 * for(i = 0; i<=n-1; i++)
 *      if(n%i == 0) not prime;
 *
 * Optimzed:
 * for(i = 0; i<=sqrt(n); i++)
 *      if(n%i == 0) not prime;
 *
 *
 */



int main(){
}

